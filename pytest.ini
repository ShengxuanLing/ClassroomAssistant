[pytest]
testpaths = tests
markers =
    integration: tests that require the real faster-whisper runtime and model download (deselect with -m "not integration")
    nightly: nightly real-engine smoke tests (real Whisper / OCR decode). Selected together with -m integration via -k nightly; skips cleanly when the real model / runtime is unavailable or its cache is unusable.
